{
  "buildCommand": "echo 'No build needed'",
  "outputDirectory": "src",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/src/$1" }
  ]
}