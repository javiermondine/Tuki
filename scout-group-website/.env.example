# Local development environment variables
# Copy this file to .env or .env.local and fill in the values

# MongoDB (optional if you only use Supabase)
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@HOST/DATABASE?retryWrites=true&w=majority

# Supabase (required)
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# IMPORTANT: use the service_role key for server-side API writes (never expose in client)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.SERVICE_ROLE_KEY_PLACEHOLDER

# Notes:
# - In Vercel, set project secrets and link them in vercel.json as:
#   supabase-url, supabase-service-role-key, mongodb-uri
# - For local scripts: `source .env.local` (or copy to .env) before running migrate/seed.# Environment variables for local development
# Copy this file to .env and fill in your credentials

MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@HOST/DATABASE_NAME?retryWrites=true&w=majority
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key